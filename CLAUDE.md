# Claude Code Instructions

## Git commits

- **Never use Claude as the commit author.** All commits must be authored as `Justin Kidd <justin@smallaxeit.com>`.
- Never append Claude session URLs to commit messages.
- Never add any Claude/Anthropic attribution, co-author lines, or references in commit messages, code comments, or documentation.
- Always work on the `development` branch unless explicitly told otherwise. Never commit directly to `master`.
